MANUAL UPLOAD

This ZIP intentionally does NOT include a `news/` directory.
Keep your existing `news/` folder and its .txt files unchanged.

Upload/merge the contents of this ZIP into the repository root.

Required GitHub Actions secret:
  OPENAI_API_KEY

Optional:
  PEXELS_API_KEY

Default model:
  gpt-5.4-nano

Optional model override:
  OPENAI_MODEL=gpt-5.4-mini

Workflow:
  .github/workflows/build-video-kit.yml

It triggers on pushes that add/change:
  news/*.txt
  app/**
  pipeline/**
  pyproject.toml
  .github/workflows/build-video-kit.yml
