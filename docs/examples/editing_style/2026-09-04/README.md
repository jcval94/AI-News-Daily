# Editing style lint — real episode replay 2026-09-04

This artifact evaluates the historical real edit-manifest replay against the current `jc_reflective_essay_v1` grammar.

It is intentionally observational. The warnings are not claims that the old edit is objectively wrong; they identify where the new channel grammar would make a different choice.

Key findings:

- presenter first appears at 20 s vs current <=6 s anchor target;
- opening contains 20 s continuous media vs current 18 s opening ceiling;
- presenter visual share is ~93.8%, above the current 85% calibration target;
- several uninterrupted presenter runs exceed the current 45 s attention-reset target.

The current production fallback now preserves an early presenter anchor when it can still retain at least five opening media opportunities.
